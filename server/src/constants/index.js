const EMAILS = require('./email_types');
const EMAIL_SUBJECTS = require('./email_subjects');
const STATUS_CODE = require('./status_code');

module.exports = {
	EMAILS,
	STATUS_CODE,
	EMAIL_SUBJECTS,
};
